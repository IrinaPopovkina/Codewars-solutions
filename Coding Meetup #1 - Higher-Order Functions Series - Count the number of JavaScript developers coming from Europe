https://www.codewars.com/kata/582746fa14b3892727000c4f/train/javascript
unction countDevelopers(list) {
  return list.filter(el => el.language === 'JavaScript' && el.continent === 'Europe').length;
}
